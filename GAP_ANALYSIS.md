# GAP_ANALYSIS — current bot vs. SPEC_FIELD_V2 (Galit field-inspection bot)

Scope: analysis and planning only. Maps the current task-management codebase
(starting state) to the v2 Galit spec (target state). No code, no migration, no
DB changes here.

---

## Top summary

- **Total gaps identified:** 30
  - Domain 1 (DB schema):                 6
  - Domain 2 (Worker — field inspections): 9
  - Domain 3 (Leads stream / Sasha):      5
  - Domain 4 (Manager digest / Yoram):    4
  - Domain 5 (Cross-cutting infra):       6
- **MVP blockers:** 12
  (the gaps without which no inspection can be created, confirmed, advanced
  through statuses, or alerted on; and without which Sasha cannot receive the
  9:30 leads digest)
- **Existing-capability fate counts (Part 2 — 12 capabilities reviewed):**
  - Kept as infrastructure (unchanged):     5
  - Rewritten/repurposed for v2:            3
  - Dropped from scope:                     2
  - Needs a decision (spec silent):         2
- **External inputs — BOTH RESOLVED (2026-07-01):**
  1. **B1 resolved** — `InspectionType` dictionary: proceed with unambiguous field-inspection מק"טים from `SPEC_FIELD_V2.md` lines 416-571. Shielding/borderline rows excluded from initial seed. `D1-T7` unblocked.
  2. **B2 resolved** — leads table: `IncomingLead`. Columns: `id`, `subject`, `body`, `fromName`, `fromEmail`, `receivedAt`, `status`, `ownerId`, `taskId`, `notifiedAt`. Assignment field: `ownerId` (UUID FK to `User`; null = unassigned). No phone — display `fromName`/`fromEmail`/`subject`/`body`. `transferredToId` not used. All Domain 3 tasks unblocked; `D4-T1` leads portion unblocked.

---

## Part 1 — v2 requirements, by domain

Each row is one gap. "Existing infra to build on" identifies the file the new
code should hang off (not duplicate). Classification reflects MVP per §14 of
the spec.

### Domain 1 — DB schema (additive only)

| Spec section | What's required | What exists today | Existing infra to build on | The delta | Classification | Blocked-on-external-input |
|---|---|---|---|---|---|---|
| §3, §14, migration block (lines 258-356, 408-413) | `InspectionType` table — dictionary keyed by מק"ט (`code` UNIQUE = `Task.productName`), `labelHe`, `family` text+CHECK across 13 values, `isActive`, `sortOrder`, `isFieldInspection` boolean (added in seed step), `createdAt/updatedAt`, RLS deny-all, family index. | Absent. `src/db/migrations/` ends at `008_digests.sql`; no inspection tables. | New table in a new migration `009_*.sql` following 001-008 conventions (`src/db/migrations/008_digests.sql` is the closest convention reference). | Whole table is new. Required for any field-inspection identity at all. | MVP blocker | No (DDL not blocked; seed content is blocked — see #6 below) |
| §3, migration block (lines 282-294) | `InspectionChecklist` table — equipment **per family** (no per-מק"ט rows): `family` text+CHECK 13 values, `code`, `labelHe`, `isRequired`, `sortOrder`, `UNIQUE(family,code)`, family index, RLS deny-all. **Note:** earlier versions had a `kind` column and a photos kind — `kind` is dropped, only equipment remains. | Absent. | New table in `009_*.sql`. | Whole table is new. Required so the morning equipment reminder (§10) can roll up per worker. | MVP blocker | No |
| §3, §4, migration block (lines 297-336) | `TaskField` table — 1:1 with `Task` (`taskId` UNIQUE FK), `inspectionTypeId` FK, snapshot `family`, static metadata (`siteAddress`, `siteCity`, `fieldContactName`, `fieldContactPhone`, `navigationUrl`, `specialInstructions`), live operational `fieldStatus` text+CHECK of **exactly 10 values** (`ASSIGNED, CONFIRMED, DECLINED, NEEDS_MORE_INFO, EN_ROUTE, ARRIVED, FINISHED_FIELD, WAITING_FOR_INFO, HAS_PROBLEM, CANCELED` — **no STARTED**), per-status timestamps (`assignedAt, confirmedAt, declinedAt, departedAt, arrivedAt, finishedAt`), `declinedReason`, inline single problem (`problemType` text+CHECK of 7 values, `problemNote`, `hasOpenProblem`), missing-info (`missingReportInfo`, `missingReportInfoNote`), `managerNotifiedAt`, `updatedByUserId` → `User`, `createdAt/updatedAt`. Status index + partial index on `hasOpenProblem`. RLS deny-all. **A Task is a field inspection IFF a row exists here.** | Absent. | New table in `009_*.sql`. | Whole table is new. This is the operational spine of the worker UX. | MVP blocker | No |
| §3, §1 ("the one approved exception") | Single flag field on `Task` (CRM table) marking "field task = yes" — the dropdown the office sets in the CRM. This flag is the trigger that auto-creates the `TaskField` row. | Absent. `src/types/index.ts` Task interface (lines 78-92) has no such field. No `ALTER TABLE "Task"` in any migration. | None — `Task` is owned by the CRM. | Spec explicitly authorizes ONE additive column on `Task`. Even the trigger/auto-creation mechanism (DB trigger? backend hook? CRM-side write of the `TaskField` row?) is not spelled out and **needs a decision** before writing migration 009. | MVP blocker (also needs-decision on mechanism) | No (column name and mechanism are our decisions) |
| §3, §14, migration block (lines 339-352) | RLS enabled deny-all on all 3 new tables (bot uses the Supabase service-role key which bypasses RLS — pattern mirrors 001-008). | Pattern exists in every migration 001-008 and is reused. | `src/db/migrations/008_digests.sql` (the cleanest recent example). | Apply same template to the 3 new tables. | MVP core | No |
| Migration block (lines 360-381 + final seed lines 580-598) | Seed `InspectionChecklist` with equipment rows for the 4 declared families (radiation, noise, asbestos, radon — totaling 17 rows). | Absent. | Same migration. | Idempotent `INSERT ... ON CONFLICT DO NOTHING`. The 4-family seed is fully specified in the spec; no waiting on external input for THIS seed. | MVP core | No |

### Domain 2 — Worker side, field inspections (sections 5-11)

| Spec section | What's required | What exists today | Existing infra to build on | The delta | Classification | Blocked-on-external-input |
|---|---|---|---|---|---|---|
| §5 main menu (worker) | Replace the worker's main menu with the v2 inspections menu: 7 items — `הבדיקות שלי להיום`, `הבדיקות שלי למחר`, `עדכון סטטוס בדיקה`, `דיווח על בעיה`, `חסר ציוד`, `חסר מידע לדוח`, `סיכום יום`. No emoji-laden CRM menu. | The wrong menu. `src/ai/menu.ts` lines 51-61 (`employeeMenu()`) returns `המשימות שלי`, `משימות להיום`, `משימות באיחור`, `דיווח על השלמת משימה`, `בקשת שינוי מועד יעד`, `הגדרות סיכום בוקר/דוח סוף יום`, `בקשה בטקסט חופשי` — all CRM task-management, none of it about inspections. | `src/ai/menu.ts` (the numbered-menu framework: `MenuRoute`, `MenuAction`, `renderMenu`, `menuItemsFor`); `src/ai/router.ts` `handleMenuReply`/`handleMenuRoute` (lines 783-822). | Rewrite `employeeMenu()` entirely; add 6 new `MenuAction` kinds (`list_inspections_today`, `list_inspections_tomorrow`, `update_inspection_status`, `report_problem`, `missing_equipment`, `missing_report_info`, `day_summary`); strip the obsolete actions from this menu (they can remain in `MenuAction` if managers still use them, see Domain 4). | MVP blocker | No |
| §6 inspection card | On `TaskField` creation, send the assigned worker a card with: type label, customer name, address, date+time, contact name+phone, equipment list (from `InspectionChecklist` for the family), navigation link, and 3 buttons (`1. מאשר`, `2. לא יכול להגיע`, `3. צריך פרטים נוספים`). | Absent. No code emits anything on `TaskField` insert (the table itself doesn't exist yet). The existing assignment alert path (`src/routes/tasks.ts` POST handler) sends a generic CRM task message via templates; not this format. | `src/whatsapp/sender.ts` `sendButtonMessage` (lines 62-83) handles up to 3 reply buttons — exactly fits the spec. `src/services/tasks.ts` for queries against `Customer`/`User`. | New code path: detect `TaskField` insert → assemble card payload → call `sendButtonMessage`. The detection mechanism (DB trigger callback vs. CRM-side hook vs. polling) is the same open question as the Task flag mechanism (Domain 1 row 4). | MVP blocker | No |
| §6, §7 inspection card replies | Map the 3 button taps to status writes on `TaskField.fieldStatus`: button 1 → `CONFIRMED` (+ `confirmedAt`); button 2 → `DECLINED` (+ `declinedAt`, ask for a short `declinedReason` via free text, then alert the office — reassignment is in the CRM); button 3 → `NEEDS_MORE_INFO` (free-text follow-up). | Absent. The current button-id handler (`src/routes/webhook.ts` line 162-170, plus the digest button payloads in `src/ai/digestCommands.ts`) only routes confirm/cancel/approve/reject of pending CRM actions. | `src/routes/webhook.ts` interactive-message handler (lines 162-170); `src/ai/digestCommands.ts` is the closest analogue of "stable payload IDs for buttons → fixed action" and is the pattern to copy. | New deterministic payload ids (e.g. `INSP_CONFIRM_<taskId>`, `INSP_DECLINE_<taskId>`, `INSP_NEED_INFO_<taskId>`); new handler module; new `conversationContext.awaiting` states for the decline-reason and need-info follow-up texts. | MVP blocker | No |
| §7 morning reminder + status flow | Morning reminder listing today's inspections numbered, with "choose a number to update status". For the chosen inspection: `departed` → `EN_ROUTE` (+ `departedAt`); `arrived` → `ARRIVED` (+ `arrivedAt`); `finished` → `FINISHED_FIELD` (+ `finishedAt`, UNCONDITIONAL) → then the 4-option follow-up menu (`אין הערות` / `יש הערות מהשטח` / `יש בעיה` / `חסר מידע לדוח`). | Absent. There is a scheduled morning *digest* (`src/scheduler/jobs/digestDispatcher.ts` lines 144-150 + `src/whatsapp/digestContent.ts` `formatEmployeeMorning` lines 71-81) but it shows CRM tasks/counts, not an inspections list with status-update affordances. There is also no status writer for `TaskField`. | `src/scheduler/jobs/digestDispatcher.ts` (the dispatcher loop, dedup, audit, advisory lock); `src/services/digestSendLog.ts` (already supports `MORNING`/`EVENING` plus the (userId, type, localDate) PK — extensible to other digest types if needed); `src/ai/router.ts` numbered-reply pattern. | New service `inspections.ts` (read today/tomorrow for a worker, advance status); new morning-reminder content formatter; rewrite of how the worker's morning digest is built — it must show today's inspections list, not generic task counts. | MVP blocker | No |
| §7 finished follow-up | After `FINISHED_FIELD` save, present the 4-option follow-up menu and route option 2 to a free-text note saved to `fieldNotes`; option 3 to the problem flow (§9); option 4 to the missing-info flow (§8). | Absent. | New code; same `MenuAction`/`awaiting` pattern as the existing menus. | New screen + 4 paths. | MVP core | No |
| §8 missing-info-for-report | "What's missing for the report?" → free text / voice → write `fieldStatus = WAITING_FOR_INFO`, `missingReportInfo = true`, `missingReportInfoNote = <text>`, set `managerNotifiedAt`, alert the office. | Absent. | `src/whatsapp/sender.ts` `sendTextMessage` for the alert; new context state for the awaiting reply. | New flow. Note voice handling per §5 / Domain 5. | MVP core | No |
| §9 report-a-problem | Sub-menu of 7 problem types (`CUSTOMER_NOT_ANSWERING / NO_ACCESS / CUSTOMER_NOT_PRESENT / MISSING_EQUIPMENT / CANNOT_PERFORM / PROFESSIONAL_ISSUE / OTHER`). Options 6 ("בעיה מקצועית") and 7 ("אחר") ask for free-text elaboration. Save `problemType`, `problemNote`, `hasOpenProblem = true`, `fieldStatus = HAS_PROBLEM`. Alert the manager in the specified format. **One problem per inspection** (multi-problem is the deferred `TaskFieldEntry`). | Absent. | Same numbered-menu pattern as `src/ai/menu.ts`. | New flow + new alert template (the spec gives the exact text in §9). | MVP core | No |
| §10 equipment reminder | Morning message that rolls up equipment across the day's inspections by joining their families to `InspectionChecklist`. 2 buttons: `לקחתי הכל` / `חסר לי ציוד`. The second option → free text → manager alert. | Absent (table doesn't exist; no aggregation code). | `src/scheduler/jobs/digestDispatcher.ts` for the dispatch wiring; `src/whatsapp/sender.ts` `sendButtonMessage`. | New scheduled job (or merged with the morning reminder); per-worker family aggregation; new button handler. | MVP core | No |
| §11 day summary (worker) | On-demand "סיכום יום" computed from today's `fieldStatus` values: list inspections that reached `FINISHED_FIELD`, plus a `WAITING_FOR_INFO` count. Then a 4-option menu (`הכל בוצע` / `חסר מידע לדוח` / `צריך לחזור ללקוח` / `בעיה פתוחה`). The options 2-4 route back into the existing missing-info / call-back / problem flows. **No DB write of "day closed" — `FieldWorkerDayClose` is deferred (§14).** | Absent. | New service in `inspections.ts`; routed from the new worker menu (Domain 2 row 1). | New screen + sub-menu. | MVP core | No |

### Domain 3 — Leads stream (Sasha) — section 12

| Spec section | What's required | What exists today | Existing infra to build on | The delta | Classification | Blocked-on-external-input |
|---|---|---|---|---|---|---|
| §12 source | Read leads from the existing `IncomingLead` table (note: **NOT** the CRM's `Lead` table — `Lead` is referenced in `src/types/index.ts` lines 109-116 and `src/services/tasks.ts` `findLeadsByName` lines 56-65 for relink purposes, but the spec is explicit that the source is a different table). The bot reads only; assignment is done in the CRM. | Absent. No code reads any "IncomingLead" table; the only Lead references are to the CRM `Lead` model used to relink CRM tasks. | New `src/services/leadsIncoming.ts`. The DB-pool pattern is established (`src/db/connection.ts` + every service in `src/services/`). | New service. **Cannot be written until the column names are confirmed.** | MVP blocker | **NO — B2 resolved 2026-07-01** |
| §12 morning digest 09:30 to Sasha | At 09:30 local, send Sasha a list of all leads received between 17:00 yesterday and 09:30 today that are still unassigned. Format per spec. Include per-lead AI suggestion of best-matching worker by ROLE. | Absent. Sasha is detected today only as a `MANAGER` getting the generic CRM manager morning digest at 08:00 (`formatManagerMorning`, `src/whatsapp/digestContent.ts` lines 83-110). | `src/scheduler/jobs/digestDispatcher.ts` (the dispatcher loop, advisory lock, audit, the (userId, type, localDate) dedup pattern via `src/services/digestSendLog.ts`); `src/ai/provider.ts` for the AI call to suggest a worker. | New digest type `LEADS_MORNING` (or a Sasha-only flavor); new content formatter; new query against `IncomingLead`. The 08:00 default in `src/db/migrations/008_digests.sql` line 27 won't fire this — needs a dedicated cron entry at 09:30 or a per-Sasha preference row. Also: §13 has Yoram seeing leads in his own digest, so leads-content is shared logic. | MVP blocker | **NO — B2 resolved 2026-07-01** |
| §12 worker-assignment alert | When `ownerId` on an `IncomingLead` row transitions from null to a `User.id`, send that worker an alert (read-only): `fromName`/`fromEmail`/`subject`/`body`, "לטיפול ועדכון ב-CRM". No phone. The worker takes no action in the bot on the lead. | Absent. No assignment-detection mechanism. | A polling job in `src/scheduler/jobs/` (mirroring `completionNotifier.ts` lines 16-37 which polls `Task.status='DONE'` and uses a dedup table) is the closest pattern. Will need a new dedup table or a `notifiedAt` column to avoid re-alerting. | New polling job + new dedup table (or column on a new bot-side mirror table). Mechanism question: same as the Task-flag mechanism in Domain 1 — DB trigger? Polling? CRM-side hook? Needs a decision. | MVP blocker | **NO — B2 resolved 2026-07-01** |
| §12 daytime escalation | Any lead created 09:30-22:00 that stays unassigned for **1 hour after its creation** → ONE alert to Sasha. Include the AI suggestion (by worker ROLE) or "לא נמצאה התאמה". Overnight leads (17:00-09:30) are NOT escalated — they're already covered by the morning digest. Exactly ONE event per lead. | Absent. | Same polling-job + dedup pattern as the assignment alert above (likely the same job runs both checks). | New job. Needs a dedup table or column to guarantee "one event per lead". | MVP core (still MVP per §14, just not strictly day-1 blocker) | **NO — B2 resolved 2026-07-01** |
| §12 AI suggestion by ROLE | Use the AI provider with a system prompt that maps a lead's `service`/message to the best `User.role` and proposes one candidate worker. SUGGESTION only — never an automatic assignment. | Absent. `src/ai/provider.ts` exists and is in use for intent parsing; this would be a separate call. | `src/ai/provider.ts` (`getProvider`); `src/ai/intentParser.ts` (pattern for building a system prompt). | New AI function (1 call per lead in the digest or per escalation). | MVP core | No (the AI call itself isn't blocked; reading the lead text to feed it IS blocked on #2) |

### Domain 4 — Manager digest / exceptions (Yoram + Sasha) — section 13

| Spec section | What's required | What exists today | Existing infra to build on | The delta | Classification | Blocked-on-external-input |
|---|---|---|---|---|---|---|
| §13 Yoram exceptions digest (morning + evening) | New content for Yoram: field totals (בוצעו / לא אושרו / עם בעיה / ממתינות למידע / לא סגרו יום) + leads (מהלילה / לא שויכו) + a numbered list of OPEN exceptions (worker + customer + free-text issue). Replaces the current `formatManagerMorning` / `formatManagerEndOfDay` CRM team summary for him. | The wrong content. `src/whatsapp/digestContent.ts` `formatManagerMorning` (lines 83-110) and `formatManagerEndOfDay` (lines 142-172) emit CRM task counts: due-today / overdue / completed / not-completed / open-carry — none of that maps to the v2 exceptions. The dispatcher (`src/scheduler/jobs/digestDispatcher.ts` lines 144-150) selects these formatters by `role === MANAGER || role === ADMIN`. | `src/scheduler/jobs/digestDispatcher.ts` (the whole dispatch + dedup + audit framework — fully reusable); `src/services/digestSendLog.ts` (already supports per-user/type/day dedup). The morning 08:00 / evening 17:00 default times in `src/db/migrations/008_digests.sql` lines 27-29 work for Yoram unchanged. | New formatters `formatGalitManagerMorning` / `formatGalitManagerEndOfDay` (or replace the existing ones entirely if Yoram is the only elevated user); new aggregation queries against `TaskField` + `IncomingLead`. **The 5 counts and the "open exceptions" list need new queries** — they don't exist in `src/services/tasks.ts`. | MVP blocker | YES for the leads numbers only; the field exceptions side does not depend on external input |
| §13 Sasha leads side | Sasha's morning 09:30 leads digest and the unassigned-lead escalations (already covered in Domain 3 above); she's a separate elevated user but with a DIFFERENT digest from Yoram (leads only, not field exceptions). | Absent — and the current code can't even distinguish her from Yoram (both are `MANAGER`/`ADMIN` and would get the same digest today). | `src/scheduler/jobs/digestDispatcher.ts`. | Either (a) per-user routing inside the dispatcher (Yoram = field+leads exceptions digest; Sasha = leads-only digest), keyed by something like a phone allow-list or a new role attribute; or (b) split into two scheduled jobs. **Spec is silent on the mechanism — needs a decision.** | MVP blocker (also needs-decision) | YES (leads side) |
| §13 Open-exceptions list inside Yoram's digest | The numbered list of "פתוחים" entries — each row is a worker + a free-text description of the open problem / missing item, drawn from `TaskField.problemNote` / `missingReportInfoNote`. | Absent (the source columns will exist only after Domain 1 row 3 is built). | New service helper that joins `TaskField` to `User`/`Customer`/`Task` and emits ordered rows. | New query + new rendering. | MVP core | No |
| §13 Removal/coexistence of the OLD CRM manager digest | Once Yoram's v2 digest replaces the current manager digest, decide whether the existing manager digest path stays (other managers / fallback) or is removed. **Spec is silent — needs a decision.** Today every `MANAGER`/`ADMIN` user gets the CRM manager digest. After v2 there will be exactly Yoram (field+leads exceptions) and Sasha (leads only) on the manager side. | The existing `formatManagerMorning` / `formatManagerEndOfDay` are sent to every elevated user. | `src/scheduler/jobs/digestDispatcher.ts` line 119 (`isElevated` branching) and `src/whatsapp/digestContent.ts` lines 83-172. | Either (a) keep the CRM manager digest as a fallback for unrecognized elevated users, (b) delete it entirely (the bot is being repurposed — the old CRM digest no longer matches Galit's business), or (c) gate it behind an env flag like `LEGACY_DAILY_SUMMARY_ENABLED` (precedent in `src/scheduler/index.ts` line 76). | needs-decision (also MVP core once decided) | No |

### Domain 5 — Cross-cutting infrastructure

| Spec section | What's required | What exists today | Existing infra to build on | The delta | Classification | Blocked-on-external-input |
|---|---|---|---|---|---|---|
| §1, §2 ROLE-based menu routing for inspectors | Same single bot, different display per ROLE. An inspector sees the inspections menu (Domain 2); Yoram sees only the exceptions digest (no menu actions); Sasha sees the leads digest. Today the only branch in `menuItemsFor` (`src/ai/menu.ts` line 77) is `isElevated`. | `src/ai/menu.ts` line 77 returns `managerMenu()` when `isElevated`, else `employeeMenu()`. Two-way branch only. Field-inspector identification is currently invisible — there's no way to tell an inspector apart from a sales rep. | `src/auth/userResolver.ts` (already loads `role` + `isElevated` from `User`); `src/types/index.ts` `UserRole` (lines 41-47: `BILLING / TECHNICIAN / SALES / EXPERT / MANAGER / ADMIN`) — note none of these is "FIELD_WORKER"/"INSPECTOR". | **OPEN QUESTION (needs-decision, flag — do not decide):** how is an inspector identified? Three options the spec leaves open: (a) reuse an existing role (e.g. `TECHNICIAN` becomes "inspector") — fewest changes, but reuses a name that might already mean something else in the CRM; (b) introduce a new role value (`INSPECTOR` or `FIELD_WORKER`) on `User.role` — additive but touches a CRM column; (c) add a per-user boolean flag (e.g. `isFieldInspector`) on a bot-side table, leaving `User.role` untouched. The §1 principle "additive-only on CRM" leans toward (c). | MVP blocker (without it the inspections menu cannot be shown to the right people) | No |
| §5, §8, §9, §11, §14 voice messages | Inbound voice (`audio`) messages must be transcribed and routed exactly like text — "departed for Ra'anana" via voice arrives in the same status path. | **Absent.** `src/routes/webhook.ts` lines 156-177: only `type === 'text'` and `type === 'interactive'` are handled; everything else (including `audio`) returns "סוג ההודעה אינו נתמך". `grep voice / audio / transcrib / whisper` finds no code under `src/whatsapp/` or `src/routes/`. The `WhatsappAuditLog` schema (migration 001 line 54) has a `transcribedMessage` column already, so the column exists but is never populated. | `src/routes/webhook.ts` `processInbound` (the dispatch on `m.type`); `src/utils/auditLog.ts` (already has the `transcribedMessage` slot). | New voice handler: download Meta audio asset → call a STT provider (Whisper API or equivalent) → feed transcript into the existing `handleIncomingMessage` text path. Need the STT credentials env var. | MVP core (the spec lists "voice messages" explicitly under §14 In-scope) | No (provider choice is ours) |
| §5 free-text/voice routes directly to statuses | "departed for Ra'anana" routes straight to `EN_ROUTE` without going through the menu. Same for "arrived", "finished", etc. | Absent. Today free text goes to the AI intent parser (`src/ai/intentParser.ts`), whose intent set is `list_tasks / get_task / create_task / edit_field / edit_duedate / reassign_task / relink_task / confirm_pending_action / decline_pending_action / team_workload / help / unknown` (`src/types/index.ts` lines 182-194) — none of these are status transitions. | `src/ai/router.ts` `handleAIMessage` entry (line 89); `src/ai/intentParser.ts`. | Add new intents (`set_field_status` with sub-types `DEPARTED / ARRIVED / FINISHED / WAITING_FOR_INFO / HAS_PROBLEM`) to the AI schema; route to the new inspections service. Also accommodate that the matched inspection might be ambiguous if the worker has several today (`task_disambig`-style flow already exists in `src/ai/router.ts` lines 286-296 — reusable). | MVP core | No |
| §1, §6 (button limit) WhatsApp button capacity | The 3-button inspection card (§6) fits Meta's 3-reply-buttons limit; the 7-option problem menu (§9) and the 7-option main menu (§5) must be NUMBERED TEXT, not interactive buttons. | The numbered-text menu approach is already in use (`src/ai/menu.ts` plus the deliberate comment in `src/ai/router.ts` lines 773-776 — "V1 is a numbered TEXT menu only, no WhatsApp interactive list messages"). | `src/ai/menu.ts` + `sendTextMessage`. | Stay numbered-text for the multi-option menus; use `sendButtonMessage` (`src/whatsapp/sender.ts` lines 62-83) only for the 3-button inspection card and equipment reminder. | MVP core (design constraint to honor) | No |
| Existing infra carries over | `src/db/connection.ts`, the migration mechanism (`src/db/migrate.ts` + `schema_migrations` tracking), the scheduler (`src/scheduler/index.ts` with advisory locks + `Asia/Jerusalem` timezone + per-job lock IDs — extend `JOB_LOCK_IDS` for new jobs), the WhatsApp sender layer (`src/whatsapp/sender.ts`), `resolveUserByPhone` (`src/auth/userResolver.ts`), the numbered-menu framework (`src/ai/menu.ts` + `handleMenuReply` in `src/ai/router.ts`), conversation-context awaiting states (`src/services/conversationContext.ts`), the inbound queue + dedup (`src/routes/webhook.ts` + migration 003), the audit log (migration 001 + `src/utils/auditLog.ts`). | All present and idempotent. | Same files. | None — these are infrastructure and stay. The new v2 code hangs off them. | infrastructure (no gap) | No |
| §12 lead-AI suggestion / §5 free-text intent parsing AI provider | The AI provider must remain available for: (a) lead-to-worker suggestion (§12), (b) routing free-text/voice to a `fieldStatus` (§5), (c) extracting `missingReportInfoNote` / `problemNote` cleanly. | `src/ai/provider.ts` exists; `src/ai/intentParser.ts` calls it; `OPENAI_API_KEY` (or similar) env var is already a deployment dependency. | `src/ai/provider.ts`, `src/ai/intentParser.ts`. | Add new schemas for the new intents and the new suggest-worker call; the underlying provider plumbing is reused. | MVP core | No |

---

## Part 2 — Fate of the existing functionality

| Name | What it does today (file) | Fate in v2 | Note |
|---|---|---|---|
| `my tasks` / `list_tasks` | Lists the caller's CRM tasks with filters (today / overdue / this_week / open / all / unlinked / next_deadline / today_overdue). `src/services/tasks.ts` `listTasks` (lines 82-210); `src/ai/router.ts` `doListTasks` (lines 641-655); `src/types/index.ts` `TaskFilter` (lines 219-227). Wired into the employee menu items 1-3. | **dropped** | The worker's UX is rewritten as "my inspections today/tomorrow" (§5 menu items 1-2), which read `TaskField` (a Task is an inspection IFF it has a `TaskField`), not generic `Task`. The free-text intent `list_tasks` becomes irrelevant for inspectors. Other CRM roles wouldn't exist in the v2 product. |
| `create_task` | AI intent to create a CRM `Task`. `src/ai/router.ts` line 413-430; `src/types/index.ts` line 188; `src/services/tasks.ts` create path. | **dropped** | Inspections are created in the CRM (the office sets the dropdown). The bot never writes `Task`. Per §1 the bot doesn't author tasks. |
| `change due date` + manager approval pipeline | Free-text "change due date" routes to `edit_duedate` → creates a `PENDING_EMPLOYEE_CONFIRM` then `PENDING_MANAGER_APPROVAL` row in `WhatsappPendingAction`, manager confirms via "אשר <uuid>" / "דחה <uuid>". `src/ai/router.ts` `edit_duedate` (lines 440-446); `src/routes/tasks.ts`; `src/services/pendingActions.ts`; `src/auth/permissions.ts` `MANAGER_APPROVAL_FIELDS` (line 7); manager menu item 6 "אישורים ממתינים". | **dropped** | Date changes on an inspection happen in the CRM (the office reschedules, then re-pushes the assignment to the worker). The bot doesn't move dates. The whole `WhatsappPendingAction` table — and the entire approval pipeline — has no purpose in v2. Could be left in the DB to avoid a DROP, but the code paths exit. |
| `team_workload` | Manager view of open-task load per employee. `src/services/tasks.ts` `getTeamWorkload` (lines 218-235); `src/ai/router.ts` `doTeamWorkload` (lines 745-770); manager menu item 1. | **dropped** | Replaced by Yoram's v2 exceptions digest (§13), which is about open field problems and missing report info, NOT generic workload counts. |
| Employee morning digest + evening digest (CRM content) | Per-user 08:00 morning ("opening day plan") and 17:00 evening ("end-of-day status") summaries of CRM `Task` counts. `src/whatsapp/digestContent.ts` `formatEmployeeMorning` (lines 71-81), `formatEmployeeEndOfDay` (lines 116-139); `src/scheduler/jobs/digestDispatcher.ts`. | **to-rewrite** | The dispatcher framework is kept as infrastructure; the CONTENT becomes: worker morning = today's inspections list + equipment reminder (§7, §10); worker "day summary" is now on-demand, not 17:00 broadcast (§11). The legacy 17:00 broadcast (`runDailySummary`) is already disabled by default (`src/scheduler/index.ts` line 76), so that path stays dormant. |
| Manager morning + evening digest (CRM content) | Per-elevated-user 08:00 / 17:00 summaries: due-today, overdue, completed, not-completed, per-employee breakdown. `src/whatsapp/digestContent.ts` `formatManagerMorning` (lines 83-110), `formatManagerEndOfDay` (lines 142-172). | **to-rewrite** | Replaced by Yoram's v2 exceptions digest (§13) — field counts (`בוצעו / לא אושרו / עם בעיה / ממתינות למידע / לא סגרו יום`) + leads numbers + open-exceptions list — and Sasha's leads-only digest. |
| Digest settings sub-menu | Lets each user toggle morning/evening digests on/off and set local times. `src/ai/router.ts` `showDigestSettings` + `handleDigestSettingsReply` + `handleDigestTimeReply` (lines 897-982); `src/services/digestPreferences.ts`; `src/db/migrations/008_digests.sql`; menu item 6 (worker) / 7 (manager). | **needs-decision** | Spec is silent on whether an inspector can mute his morning reminder or an executive can retime his exceptions digest. The MECHANISM (the `UserDigestPreference` table) is infrastructure and stays. The MENU EXPOSURE — whether item 6/7 still appears in the rewritten worker menu (Domain 2 row 1) — is the open question. Default: keep as a hidden capability for now, surface only if asked. |
| Existing AI router + intent parser | Routes inbound text via threshold-based confirm/clarify, manages context, dispatches to internal HTTP routes. `src/ai/router.ts` (whole file, 1271 lines); `src/ai/intentParser.ts`; `src/ai/schema.ts`. | **to-rewrite** | The orchestration shell (provider check → context lookup → menu-trigger → fall-through to parser → context-driven follow-ups) is reusable and stays. The INTENT SET is rewritten: the CRM intents (`list_tasks`, `create_task`, `edit_field`, `edit_duedate`, `reassign_task`, `relink_task`, `team_workload`, `confirm_pending_action`, `decline_pending_action`) all drop; new intents added: `set_field_status` with sub-types DEPARTED / ARRIVED / FINISHED, `report_problem`, `report_missing_info`, plus the menu-trigger keywords. `help` and `unknown` stay. The clarification/confirmation/disambiguation patterns (`awaiting: 'menu' / 'missing_field' / 'task_disambig'` in `src/services/conversationContext.ts` lines 7-12) are reused as-is. |
| Numbered-menu framework | The pure `menuItemsFor` / `renderMenu` / `MENU_TRIGGER_RE` machinery + the `handleMenuReply` / `handleMenuRoute` plumbing. `src/ai/menu.ts` + `src/ai/router.ts` lines 778-822. | **infrastructure (kept unchanged)** | The framework itself is product-neutral. Only the menu CONTENT (Domain 2 row 1, Part 1) is rewritten. |
| Inbound queue + dedup + per-phone rate limit | Durable webhook ingestion with HMAC verification, per-phone 20/60s rate limit, queue-table dedup, recovery sweep. `src/routes/webhook.ts`; `src/services/inboundQueue.ts`; `src/db/migrations/003_inbound_queue.sql`. | **infrastructure (kept unchanged)** | Pure plumbing. All new inspection messages flow through this. |
| WhatsApp sender (text + buttons + templates) | `sendTextMessage`, `sendButtonMessage` (3 reply buttons — exactly fits §6), `sendTemplateMessage`, retry+DLQ. `src/whatsapp/sender.ts`. | **infrastructure (kept unchanged)** | Reused for every new v2 message. New approved templates will need to be registered with Meta for out-of-window sends (the §6 inspection card + §13 exception alerts will likely arrive out-of-window) — that's a config task, not a code change. |
| Per-user daily greeting | Greets each user once per local day on their first message. `src/services/greetings.ts`; `src/db/migrations/005_user_greetings.sql`; `src/routes/webhook.ts` lines 280-288. | **needs-decision** | Spec is silent. A worker getting a "שלום דני" before the v2 menu (§5) seems consistent with the spec's tone (the §5 example is literally "שלום דני, מה תרצה לעשות?"). Recommend: keep, and let the greeting open the v2 inspections menu. |
| Audit log + reminder log + completion-notification log | Generic logging tables (`WhatsappAuditLog`, `WhatsappReminderLog`, `WhatsappCompletionNotification`). | **infrastructure (kept unchanged)** | The audit log is reused for every new path; `WhatsappCompletionNotification` is no longer used by the bot once `completionNotifier` is dropped (the bot no longer detects DONE), but the table can stay. |

---

## Out-of-scope — intentionally deferred per Section 14

Listed for completeness only — NOT entered as gaps.

- Photos (no upload, no completion gate, no `TaskPhotoMeta`).
- Outlook integration.
- `TaskFieldStatusHistory` (the structured status-history table).
- Structured `TaskFieldEntry` (multi-problem-per-inspection).
- `FieldWorkerDayClose` (the "I'm done for the day" sealed record).
- Performance analysis.
- Automated reports.
- Lead actions from within the bot (assignment / handling).
